namespace Coplt.UI.Styles;

public enum AreaAxis : byte
{
    Row,
    Column,
}

public enum AreaEnd : byte
{
    Start,
    End,
}

public enum GridAutoFlow : byte
{
    Row,
    Column,
    RowDense,
    ColumnDense,
}
